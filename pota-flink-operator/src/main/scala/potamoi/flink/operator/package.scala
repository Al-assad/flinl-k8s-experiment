package potamoi.flink

package object operator {
  val flinkRest       = FlinkRestRequest
  val clDefResolver   = ClusterDefResolver
  val podTplResolver  = PodTemplateResolver
  val logConfResolver = LogConfigResolver
}
